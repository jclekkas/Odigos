Implement Task #33: Analyzer upload visibility fix.

## PROBLEM

The analyzer page currently supports document/image upload, but the upload entry point is visually buried as tiny underlined helper text beneath the textarea:

"or upload a screenshot or PDF"

This makes users think the upload feature does not exist, even though the backend and frontend wiring are intact.

## PRIMARY GOAL

Make upload a first-class, obvious option in the analyzer UI by turning the existing analyzer input area into a two-tab interface:

- Paste text
- Upload

This is a presentation and layout change only.
Do NOT change analyzer logic, upload logic, OCR flow, backend routes, or API behavior.

## CONFIRMED FACTS (SOURCE OF TRUTH)

- Route is correct: /analyze → client/src/pages/home.tsx
- Upload UI and handler already exist in home.tsx
- Upload backend is intact:
  - POST /api/extract-text
  - multer middleware
  - server/extractText.ts
- Wiring is intact
- Root cause is only that the upload affordance is too visually hidden

## SCOPE

Only modify:

- client/src/pages/home.tsx

Do not touch any other file.

## REQUIREMENTS

Convert the current analyzer input area into a proper two-tab interface using the existing component system.

### Tab requirements
- Two tabs only:
  - Paste text
  - Upload
- Tabs must be visually prominent and full width
- TabsList should span the full width of the analyzer card/container
- Each TabsTrigger should take equal width (50/50)
- Upload should feel like a primary supported workflow, not a footnote

### Paste text tab
- Keep the existing textarea
- Keep existing placeholder/copy unless a tiny wording adjustment is needed for clarity
- Keep existing submit behavior, validation, tracking, and state exactly as-is

### Upload tab
- Reuse the existing hidden file input
- Reuse the existing handleFileUpload logic exactly as-is
- Present upload as a clear primary affordance
- Make accepted file types clearly visible:
  - PNG, JPG, JPEG, WEBP, PDF
- Keep loading and error states exactly as they already work
- Do not alter the backend request or endpoint

## CRITICAL CONSTRAINTS

Do NOT:
- change analyzer logic
- change upload handler logic
- change OCR or extraction flow
- change backend files
- change routes
- change API contracts
- change pricing
- change CTA destinations
- add new libraries
- redesign the whole page
- auto-commit or auto-merge

## IMPLEMENTATION GUIDANCE

- Use the existing Tabs/TabsList/TabsTrigger/TabsContent pattern already available in the app if present
- Keep changes localized and reviewable
- Preserve accessibility
- Preserve all current state and behavior
- This should be a visibility/IA improvement, not a functional rewrite

## DELIVERABLES

Do NOT commit or merge.

Return:
1. exact file modified
2. full unified diff
3. summary of what changed
4. confirmation that no logic or backend behavior changed
5. confirmation that upload still uses the exact same handler and endpoint as before