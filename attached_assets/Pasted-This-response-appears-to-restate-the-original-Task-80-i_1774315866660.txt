This response appears to restate the original Task #80 implementation, not the requested hardening pass for #80.1.

The following required changes are still unconfirmed and likely not implemented:

1. A new formatIssueLabel(field: string) mapping layer to convert raw missingInfo[].field values into human-readable issue text
2. Stronger sanitization that also strips percentages and other numeric patterns, not just dollar amounts and long numbers
3. Copy button success/failure UX feedback (for example: "Copied ✓" then revert)
4. Zero-issue fallback handling in both copied text and exported PNG
5. PNG rendering hardening for reliability across Chrome, Firefox, and Safari
6. Exact copied summary format polish and example output
7. Confirmation that issue text is no longer mechanical/raw-key-like

Please implement the actual #80.1 hardening pass now.

Task requirements:

- Keep scope frontend-only
- Do not add dependencies
- Do not redesign the feature
- Modify only:
  - client/src/components/ShareCard.tsx
  - client/src/pages/home.tsx

Specific required changes:

1) Add formatIssueLabel(field: string): string
Use it for both copied summary and ShareCard issue display.

Examples:
- apr_missing -> APR is missing — total cost is unclear
- doc_fee_high -> Documentation fee appears unusually high
- otd_missing -> Full out-the-door price not provided
- addons_unclear -> Add-ons may not be optional

Fallback:
- replace underscores with spaces
- trim
- capitalize first letter

2) Strengthen sanitizeIssueLabel()
Strip:
- dollar amounts
- percentages
- long numeric strings
- number-heavy fragments

No exact numeric deal details may appear in copied text or PNG.

3) Add copy UX feedback
After successful copy:
- button temporarily changes to "Copied ✓"
- revert after about 1.5–2 seconds

If copy fails:
- temporary "Copy failed" feedback

4) Add zero-issue fallback
If no issues are available after formatting/sanitization, use exactly:

No major issues surfaced in this summary

Apply this to both:
- copied summary
- PNG scorecard

5) Harden PNG export reliability
Ensure:
- explicit 1200x630 dimensions
- solid background
- off-screen render only, not display:none
- no clipped text
- capture waits until the node is mounted/styled
- reliable rendering in Chrome, Firefox, and Safari

6) Ensure copied summary uses exactly this format:

Odigos Deal Scorecard
Verdict: [GO / NO-GO / NEED-MORE-INFO]
[verdictLabel]

Issues flagged:
• [issue 1]
• [issue 2]
• [issue 3]

Analyzed by Odigos — odigos.replit.app

If no issues:
Issues flagged:
• No major issues surfaced in this summary

Return:
1. Exact files changed
2. The new formatIssueLabel and sanitizeIssueLabel logic
3. Confirmation that no numeric deal details appear anywhere
4. Confirmation of browser validation for Chrome, Firefox, and Safari
5. One example of the final copied summary output