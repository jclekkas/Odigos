Implement Task #135 in the existing Odigos codebase. This is an implementation task, not a planning task.

Objective
Refactor the analyze route into a thin HTTP layer backed by a service module, and remove unused auth/session dependencies.

Context
The current file server/routes/analyze.ts is ~752 lines and mixes:
- HTTP handling
- AI orchestration
- state detection
- circuit-breaker logic
- warehouse logging/enqueueing

This should be split so the route becomes thin and the business logic lives under server/services/.

Done looks like
- The analyze route handler is 150 lines or fewer
- All business logic lives in one or more dedicated service modules under server/services/
- The existing API tests for /api/analyze continue to pass without modification
- passport, passport-local, and express-session are removed from package.json and absent from the codebase
- TypeScript compiles cleanly with no new errors

Out of scope
- Adding authentication
- Adding sessions
- Changing the public API shape of /api/analyze
- Adding new tests unless strictly required to preserve existing behavior

Files to inspect first
- server/routes/analyze.ts
- server/routes/index.ts
- server/index.ts
- package.json

Required implementation
1. Extract an AnalyzeService
Create server/services/analyzeService.ts as the main orchestration layer for /api/analyze.

Move out of server/routes/analyze.ts:
- AI orchestration
- state detection
- circuit-breaker behavior
- warehouse enqueueing/logging
- other non-HTTP business logic currently embedded in the route

Keep in server/routes/analyze.ts:
- request parsing
- input validation
- calling the service
- mapping service result to HTTP response
- preserving existing status codes and response JSON shape
- translating errors into the same HTTP behavior as today

2. Remove dead auth dependencies
Remove these packages from package.json:
- passport
- passport-local
- express-session

Then search the entire repo and remove any related:
- imports
- setup/config
- middleware
- dead references
- unused code paths

Hard constraints
- Do not change the request or response shape of /api/analyze
- Do not change existing tests
- Do not do unrelated cleanup
- Do not introduce auth/session replacements
- Prefer minimal, surgical refactoring over redesign
- Preserve all current side effects and error semantics

Suggested structure
- server/routes/analyze.ts
  - thin route only
- server/services/analyzeService.ts
  - main analyze orchestration entrypoint
- optional helper modules under server/services/ only if truly needed

Implementation guidance
- Reuse existing logic rather than rewriting behavior
- Extract logic as-is where possible
- Keep naming simple and obvious
- Avoid speculative abstractions
- If helper functions are needed, colocate them under server/services/
- Route file must finish at 150 lines or fewer

Verification steps you must run
1. Search repo for remaining references to:
   - passport
   - passport-local
   - express-session
2. Run the existing /api/analyze tests unchanged
3. Run TypeScript typecheck/compile
4. Run build if available
5. Confirm final line count of server/routes/analyze.ts

Acceptance criteria
- server/routes/analyze.ts is <=150 lines
- Business logic has been moved into server/services/
- Existing /api/analyze tests pass unchanged
- passport, passport-local, and express-session are removed from package.json and no longer referenced anywhere
- TypeScript compiles with no new errors
- Build passes if a build command exists

When finished, report only:
1. Exact files created/modified
2. What logic moved into the service layer
3. Final line count of server/routes/analyze.ts
4. Test results
5. Typecheck/build results
6. Confirmation that passport, passport-local, and express-session were fully removed