🟡 Final Edge-Case Fixes (worth doing)
1) Prevent undefined/NaN rendering in UI
Right now you say:
compute the delta
But you don’t guard against:
missing doc fee
non-numeric values
NaN propagation
Add this line:
Only compute and render the delta when both values are valid finite numbers. Otherwise, fall back to rendering the average without comparison.
2) Ensure consistent currency formatting
Right now examples use $312, $340.
Replit might:
forget formatting
mix integers/strings
render raw numbers
Add:
All monetary values should be formatted as USD with a dollar sign and no decimals (e.g., $312).
3) Lock down pluralization / grammar (small but high polish)
You use:
“847 deals”
“5 analyzed quotes”
LLMs + code often mess this up (1 deals, etc.)
Add:
Handle singular vs plural correctly (e.g., “1 deal” vs “847 deals”).
4) Prevent duplicate prompt injection
This is a subtle one.
If someone refactors later, you could end up appending the block twice.
Add:
Ensure the MARKET_INTELLIGENCE block is appended only once per request.
5) Explicitly forbid leaking internal field names to the LLM
Right now you're safe, but worth locking:
Add:
Do not expose internal field names, database column names, or schema identifiers in the prompt. Use only plain-English phrasing.
6) Guard against empty-but-non-null marketContext
Edge case:
marketContext = {}
That would still trigger prompt injection incorrectly.
Add:
Only inject the MARKET_INTELLIGENCE block when marketContext contains at least one usable statistic.
🔥 Final Add-On (drop-in)
You can append this to your prompt:
Additional requirements:
- Only compute and render comparisons when all required values are valid finite numbers
- Format all monetary values as USD (e.g., $312, no decimals)
- Handle singular vs plural correctly (e.g., “1 deal” vs “847 deals”)
- Ensure the MARKET_INTELLIGENCE block is appended only once per request
- Only inject the block when at least one usable statistic is present
- Do not expose internal schema or field names in the prompt