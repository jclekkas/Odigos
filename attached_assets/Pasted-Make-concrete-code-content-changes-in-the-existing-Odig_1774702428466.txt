Make concrete code/content changes in the existing Odigos codebase. This is a ranking optimization task, not general SEO cleanup. Execute changes directly in the repo, validate them, and report results.

Context
Google Search Console shows multiple pages ranking between positions 5–15 for high-intent queries related to:
- dealer fees
- doc fee caps by state
- out-the-door (OTD) pricing
- dealer behavior (refusing OTD price, hidden fees)

Goal
Push selected pages into top 3 rankings by increasing:
1) query coverage (capture more variations of real searches)
2) semantic depth (answer adjacent intent better than competitors)
3) topical authority signals (internal linking + structured clarity)

DO NOT:
- rewrite entire pages
- add fluff
- expand low-priority pages
- touch unrelated parts of the site

FOCUS PAGES ONLY
- /dealer-wont-give-otd-price (primary target)
- /dealer-doc-fee
- Texas, New York, California state pages

---

1. QUERY EXPANSION (HIGH IMPACT)

For each focus page:
Add a new section titled:

“Common Questions About [topic]”

This is NOT a generic FAQ. It must target real search variations.

Add 4–6 tightly scoped questions per page based on patterns like:
- “Is [X] legal in [state]?”
- “Can dealers [behavior]?”
- “What happens if [scenario]?”
- “Do you have to pay [fee]?”

Examples (adapt per page):
- “Can a dealer refuse to give an out-the-door price?”
- “Are doc fees negotiable in Texas?”
- “What fees are illegal in New York dealerships?”
- “Can a dealer add fees after you agree on price?”

Rules:
- Each question must target a slightly different search intent
- Each answer must be 2–4 sentences max
- No duplication across pages
- Must integrate naturally with page topic

---

2. COMPETITOR GAP FILLING (CRITICAL)

Within each focus page:
Add ONE short section that competitors usually miss.

Examples:
- “Dealer tricks to watch for”
- “How this actually shows up on a real invoice”
- “What to say to push back”

Requirements:
- Must be actionable (not informational only)
- Must differentiate page from generic SEO content
- Keep it under ~150 words

---

3. INTERNAL AUTHORITY SIGNALING

Strengthen internal linking specifically to boost ranking power:

Rules:
- Add 2–3 links per page MAX
- Anchor text must match real queries

Examples:
- “calculate your out-the-door price”
- “dealer doc fee limits by state”
- “Texas dealer fee rules”

Required:
- All state pages must link to:
  → /car-dealer-fees-by-state
- /dealer-doc-fee must link to at least 2 state pages
- /dealer-wont-give-otd-price must link to:
  → /calculate-out-the-door-price

Do NOT:
- repeat identical anchor text multiple times
- create link spam sections

---

4. ENTITY + TERM ENRICHMENT (SUBTLE BUT IMPORTANT)

Within content (not as a list), ensure inclusion of related terms:

Examples:
- “documentation fee”
- “dealer prep fee”
- “government fees (tax, title, registration)”
- “out-the-door price”

Rules:
- Must be naturally integrated into sentences
- Do not keyword stuff
- Do not add standalone keyword lists

---

5. SNIPPET OPTIMIZATION (FOR CTR + FEATURED SNIPPETS)

Add ONE short “direct answer” block near the top of each focus page:

Format:
- 2–3 sentence clear answer to the main question

Example:
“If a dealer won’t give you an out-the-door price, it usually means they’re planning to add fees later. You can legally request a full breakdown including taxes and fees before agreeing to anything.”

Rules:
- Must directly answer the core query
- Must be concise and authoritative
- Must not contradict page content

---

6. STRUCTURED DATA (ONLY IF APPLICABLE)

If the page now includes a “Common Questions” section:
- Add FAQ structured data using existing utilities
- Must exactly match visible content

Do NOT:
- add schema without visible questions
- duplicate existing schema types

---

7. MINIMUM IMPACT REQUIREMENT

For EACH focus page, you must:
- add at least 1 new section (query expansion)
- add at least 2 new internal links
- add 1 snippet-style direct answer block

If these are not met, continue improving.

---

8. VALIDATION

After changes:
- run build / typecheck / tests if available
- confirm:
  - no duplicate canonical tags
  - no broken imports
  - no runtime errors

---

DELIVERABLE FORMAT

Return:

1. Files changed
2. For each page:
   - new sections added
   - questions added
   - internal links added
3. confirmation of schema updates
4. build/test results

---

SUCCESS CRITERIA

- Pages better cover real user queries
- Content is more actionable than competitors
- Internal linking strengthens topical authority
- No SEO regressions introduced

Execute fully. Do not stop at analysis.