Implement Phase 2A of the Odigos UI migration.

I am providing the v0 source for the target presentation. Use it as the visual/presentation reference only.

## PRIMARY GOAL

Migrate only these landing-page presentation areas toward the provided v0 design:

- header / navigation
- hero
- hero trust/supporting strip
- footer

This is a presentation transplant inside the existing Replit app.
Do NOT import or recreate the v0 app architecture.
Do NOT introduce Next.js patterns into the current app.

## CRITICAL CONSTRAINTS

Do NOT:
- change analyzer logic
- change upload flow
- change OCR / extraction flow
- change GPT/OpenAI flow
- change Stripe/payment flow
- change routes
- change API contracts
- touch backend files
- redesign the analyzer page
- touch any non-landing page except directly related presentational components
- do a global CSS/token rewrite
- change copy outside these migrated sections unless absolutely required to preserve existing app behavior or existing CTA destinations
- introduce any new features
- import the new migration placeholder files unless they are actually used for these landing-page sections

## PRESERVE BEHAVIOR

The existing Replit app is the behavior source of truth.
The v0 code is the presentation source of truth.

If there is any conflict:
- preserve current Replit behavior
- adapt the v0 presentation safely to the current app

That means:
- preserve current route behavior
- preserve current CTA behavior/destinations unless the destination already cleanly exists in the current app
- preserve accessibility and semantic HTML
- preserve working navigation behavior

## IMPLEMENTATION NOTES

Adapt the provided v0 code to the current Replit architecture safely.

Specific expectations:
- Replace `next/link` with the routing/link approach already used in this app
- Reuse existing button/component primitives where sensible
- Keep the diff localized and reviewable
- Prefer localized class/layout changes over broad shared-system changes
- If needed, create or update only directly related presentational components for the landing page
- Do not force a broad component refactor just to match v0 structure

## TARGET V0 SOURCE

### Header
"use client"

import Link from "next/link"
import { Button } from "@/components/ui/button"

export function Header() {
  return (
    <header className="sticky top-0 z-50 w-full border-b border-border/80 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
      <div className="mx-auto flex h-16 max-w-4xl items-center justify-between px-4 sm:px-6">
        <Link href="/" className="flex items-center gap-2">
          <span className="font-serif text-3xl font-extrabold tracking-tight">Odigos</span>
        </Link>
        
        <nav className="hidden items-center gap-6 md:flex">
          <Link href="/#features" className="text-sm text-muted-foreground transition-colors hover:text-foreground">
            What We Review
          </Link>
          <Link href="/#pricing" className="text-sm text-muted-foreground transition-colors hover:text-foreground">
            Pricing
          </Link>
          <Link href="/#faq" className="text-sm text-muted-foreground transition-colors hover:text-foreground">
            Questions
          </Link>
        </nav>

        <Button size="sm" asChild>
          <Link href="/analyze">Review a Quote</Link>
        </Button>
      </div>
    </header>
  )
}

### Hero
import Link from "next/link"
import { Button } from "@/components/ui/button"
import { ArrowRight } from "lucide-react"

export function Hero() {
  return (
    <section className="py-28 sm:py-40 lg:py-48">
      <div className="mx-auto max-w-4xl px-4 sm:px-6">
        <div className="mx-auto max-w-3xl text-center">
          <p className="mb-8 text-xs font-medium tracking-widest uppercase text-muted-foreground">
            Independent Quote Review
          </p>
          
          <h1 className="font-serif text-balance text-4xl font-semibold tracking-tight sm:text-5xl lg:text-6xl">
            Understand your dealer quote before you commit.
          </h1>
          
          <p className="mx-auto mt-10 max-w-2xl text-pretty text-base text-muted-foreground sm:text-lg leading-relaxed">
            Odigos reviews car dealer quotes for missing pricing details, unclear fees, 
            and language that may not be in your best interest. Paste a quote and 
            receive an independent assessment.
          </p>

          <div className="mt-14 flex flex-col items-center justify-center gap-4 sm:flex-row">
            <Button size="lg" className="gap-2" asChild>
              <Link href="/analyze">
                Review a Quote
                <ArrowRight className="h-4 w-4" />
              </Link>
            </Button>
            <Button variant="outline" size="lg" asChild>
              <Link href="/analyze?demo=true">See an Example</Link>
            </Button>
          </div>

          <p className="mt-10 text-sm text-muted-foreground">
            No account required. Takes about 10 seconds.
          </p>

          <div className="mt-12 flex flex-col items-center justify-center gap-3 text-xs text-muted-foreground sm:flex-row sm:gap-6">
            <div className="flex items-center gap-2">
              <span className="text-primary">✓</span>
              <span>Independent analysis</span>
            </div>
            <div className="flex items-center gap-2">
              <span className="text-primary">✓</span>
              <span>No dealership affiliations</span>
            </div>
            <div className="flex items-center gap-2">
              <span className="text-primary">✓</span>
              <span>Personal information redacted</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}

### Footer
import Link from "next/link"

export function Footer() {
  return (
    <footer className="border-t border-border py-10">
      <div className="mx-auto max-w-4xl px-4 sm:px-6">
        <div className="flex flex-col items-center justify-between gap-6 sm:flex-row">
          <div>
            <span className="font-serif font-semibold">Odigos</span>
            <p className="mt-1 text-xs text-muted-foreground">
              Independent dealer quote review
            </p>
          </div>
          
          <nav className="flex flex-wrap items-center justify-center gap-x-6 gap-y-2 text-sm text-muted-foreground">
            <Link href="#features" className="transition-colors hover:text-foreground">
              What We Review
            </Link>
            <Link href="#pricing" className="transition-colors hover:text-foreground">
              Pricing
            </Link>
            <Link href="#faq" className="transition-colors hover:text-foreground">
              Questions
            </Link>
            <Link href="/privacy" className="transition-colors hover:text-foreground">
              Privacy
            </Link>
          </nav>
        </div>
        
        <div className="mt-8 pt-6 border-t border-border text-center text-xs text-muted-foreground leading-relaxed">
          <p>
            Odigos reviews pricing transparency and disclosure practices in dealer quotes. 
            It does not provide legal, financial, or mechanical advice. 
            Consult appropriate professionals for those assessments.
          </p>
        </div>
      </div>
    </footer>
  )
}

## SCOPE OF WORK

Implement only the landing-page presentation changes needed to move the current app toward this design for:
- header
- hero
- hero trust strip
- footer

Do not touch pricing, FAQ, sample analysis, other marketing sections, analyzer page, uploads, backend, or payment flow in this task.

## REVIEW STANDARD

Do not claim completion unless:
- the landing page visibly moves toward the provided v0 design
- behavior is unchanged
- no backend files were modified
- no route/API/payment/upload/analyzer behavior changed
- no new TypeScript or console errors were introduced

## REPORT BACK WITH

1. exact files modified/created
2. which of the 4 target areas were migrated
3. any necessary adaptations made from Next.js/v0 to the current Replit app
4. confirmation that no backend, route, analyzer, upload, OCR, or Stripe files were changed
5. confirmation the landing page still loads cleanly with no new TypeScript or console errors