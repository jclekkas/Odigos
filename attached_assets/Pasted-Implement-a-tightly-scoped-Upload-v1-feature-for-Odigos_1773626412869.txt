Implement a tightly-scoped Upload v1 feature for Odigos.

This is a high-risk change, so follow these instructions exactly.

PRIMARY GOAL
Add the ability for a user to upload:
- one image file (png, jpg, jpeg, webp), OR
- one PDF file

Then extract the text from that file and pass the extracted text into the EXISTING analysis flow.

This feature is intended only to reduce input friction.
It is NOT a product redesign.
It is NOT a new workflow.
It is NOT a backend refactor.

NON-NEGOTIABLE CONSTRAINTS
- Do NOT change pricing
- Do NOT change the current analyzer logic or analysis prompt
- Do NOT redesign the landing page
- Do NOT add user accounts
- Do NOT add persistent file storage
- Do NOT add a document history
- Do NOT add dashboards
- Do NOT add multi-file upload
- Do NOT add drag-and-drop unless it is extremely lightweight and does not alter layout meaningfully
- Do NOT add any new pages or routes unless absolutely required for the existing architecture
- Do NOT change existing copy except where necessary to introduce upload support
- Do NOT remove the existing paste-text workflow
- Do NOT modify the existing core result experience
- Do NOT introduce large dependencies unless strictly necessary
- Do NOT touch unrelated files
- Do NOT make visual/UI changes beyond what is required to expose upload cleanly

PRODUCT BEHAVIOR
Keep the current text input flow exactly as it is, but add a secondary option that lets users upload a file.

Desired user experience:
1. User can still paste dealer quote text exactly as before
2. User can alternatively upload one screenshot/photo/PDF
3. The system extracts text from the uploaded file
4. The extracted text is sent through the SAME existing analysis path used for pasted text
5. If extraction fails or yields almost no text, show a simple, calm error message and do not crash
6. If upload succeeds, do not expose technical OCR details to the user unless needed for error handling

STRICT UX REQUIREMENTS
- The paste-text path remains primary and intact
- Upload should appear as a simple adjacent/secondary option, not a new complex widget
- The page should still feel minimal, fast, and trusted
- Avoid “AI demo” vibes
- No flashy UI
- No animations unless already part of the existing design system
- No modals unless required by current architecture
- No dark pattern wording
- No clutter

ALLOWED FILE TYPES
- PNG
- JPG
- JPEG
- WEBP
- PDF

UPLOAD LIMITS
- Support only a single file upload
- Add a reasonable file size limit
- If file is too large, show a short human message
- If file type is unsupported, show a short human message

IMPLEMENTATION APPROACH
Reuse the existing analysis pipeline as much as possible.

The uploaded file should be converted into text before analysis.
That means:
- If image: extract readable text from the image
- If PDF: extract readable text from the PDF; if direct text extraction is unavailable in the current stack, fall back to rendering pages or using the safest minimal extraction path available within the existing architecture

IMPORTANT:
Do not build a separate “document analysis” system.
Do not create a parallel analysis experience.
Everything should funnel into the current text-based analyzer.

ARCHITECTURE RULES
- Minimize code footprint
- Prefer small, composable changes
- Reuse existing components/functions where possible
- Keep responsibilities separated clearly:
  - upload selection
  - file validation
  - text extraction
  - handoff into existing analyzer
- If a helper/util is needed, keep it narrowly scoped and well named
- If a server endpoint is required, make the smallest possible addition
- Avoid broad refactors

ERROR HANDLING
Must handle these cases gracefully:
- unsupported file type
- file too large
- extraction failure
- extracted text empty or nearly empty
- generic processing failure

Error copy should be plain and calm, for example:
- “That file type isn’t supported.”
- “That file is too large to process.”
- “We couldn’t read enough text from that file. Try pasting the text or uploading a clearer image.”

SECURITY / PRIVACY
- Do not store uploaded files permanently
- Do not create a document library
- Process the file only as needed for the current analysis request
- Keep the privacy posture simple and conservative

COPY CHANGES
Only make the minimum copy changes needed to support upload.
Examples of acceptable changes:
- Add a small “or upload a screenshot / PDF” line near the existing input
- Add a simple upload button label such as “Upload screenshot or PDF”

Do not rewrite the hero.
Do not rewrite trust messaging.
Do not rewrite results copy.
Do not add marketing sections.

DESIGN REQUIREMENTS
Match the existing Odigos visual style.
The upload control should feel native to the current interface.
Maintain the site’s serious, calm, trustworthy tone.
Do not introduce a different design language.

TESTING / VALIDATION
After implementation, verify all of the following:
1. Existing paste-text flow still works exactly as before
2. Image upload works and reaches the same analyzer flow
3. PDF upload works and reaches the same analyzer flow
4. Invalid file types are rejected
5. Oversized files are rejected
6. Extraction failure does not crash the page
7. Build passes
8. No unrelated pages changed
9. No existing routes broken
10. No layout regressions on mobile

OUTPUT FORMAT I WANT FROM YOU
Before making changes, first inspect the current codebase and identify the exact smallest set of files that truly need modification.

Then implement only those changes.

When finished, give me:
1. a concise summary of exactly what files were changed
2. a concise explanation of how upload now works end-to-end
3. any dependency added, with justification
4. any tradeoffs or limitations
5. a short manual QA checklist
6. confirmation that no unrelated product logic or copy was changed

DECISION RULE
When choosing between:
A) the faster/smaller implementation that preserves the current product, and
B) the more ambitious implementation with broader changes,

always choose A.

If there is any uncertainty, preserve the current Odigos behavior and make the smallest safe change.