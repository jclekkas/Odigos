Implement Phase 4 of the Odigos UI migration: Light Theme + Header Brand Alignment.

This phase corrects the remaining fidelity gap between the current Replit app and the target v0 screenshot.

## PRIMARY GOAL

Bring the app visually in line with the target cream/light screenshot by doing two things only:

1. Ensure the app renders in the intended light/cream presentation instead of the current dark/navy presentation
2. Replace the current image logo in the header with a serif text wordmark: "Odigos"

This is a presentation-only correction.
Do NOT change product behavior, routes, copy meaning, or business logic.

## TARGET VISUAL DIRECTION

Match the feel of the provided target screenshot:
- light cream / warm off-white overall background
- clean serif wordmark "Odigos" in the header
- no dark/navy full-page presentation
- calm editorial / premium tone
- preserve the existing layout and sections already migrated in prior phases

## CRITICAL CONSTRAINTS

Do NOT:
- change analyzer logic
- change upload flow
- change OCR / extraction flow
- change GPT/OpenAI flow
- change Stripe/payment flow
- change routes
- change API contracts
- touch backend files
- change CTA destinations
- rewrite section copy
- redesign page structure
- add new features
- auto-commit or auto-merge anything

## FILE SCOPE

Prefer changes limited to:
- client/src/index.css
- client/src/components/SiteHeader.tsx

Only touch another file if absolutely necessary to correct the light-theme rendering, and explicitly report it.

## HEADER / LOGO RULE

Replace the current image logo in the header with a serif text wordmark:
Odigos

Requirements:
- use text, not an image asset
- visually match the target screenshot as closely as possible
- keep the existing header layout behavior intact
- keep the logo linked to home as it is now
- preserve existing navigation and CTA behavior
- remove the old image-logo import/usage cleanly if no longer needed

## THEME / LIGHT RENDERING RULE

The current app is still presenting in a dark/navy look that does not match the target screenshot.

Correct that.

Requirements:
- ensure the app presents in the intended light/cream theme
- do not make this a broad redesign
- use the smallest possible changes to eliminate the dark/navy presentation
- preserve readability and contrast
- keep dark mode untouched unless the incorrect dark presentation is being forced by code and must be removed from the light-mode path
- if a specific class, wrapper, or forced dark treatment is causing the mismatch, fix that directly and minimally

## IMPLEMENTATION RULES

- Prefer minimal, reviewable changes
- Do not introduce random one-off overrides across many files
- Do not hardcode colors everywhere
- Preserve the token-based design system where possible
- Keep the diff tight and intentional
- If the dark/navy mismatch is caused by a specific component or wrapper, report that clearly

## OUT OF SCOPE

- copy rewrites
- pricing changes
- FAQ changes
- sample output wording changes
- analyzer structural redesign
- new pages
- backend or routing work

## DELIVERABLES

Do NOT commit or merge.

Return:
1. exact files modified
2. full unified diff
3. explanation of what was causing the dark/navy mismatch
4. explanation of how the light/cream rendering was corrected
5. explanation of how the header logo treatment was updated
6. confirmation that no logic, route, backend, upload, OCR, or Stripe behavior changed
7. confirmation that no commit was made