Implement a tightly scoped follow-up UI/UX polish pass for Odigos based on the latest production verification audit.

This is a small refinement pass only.
Do NOT redesign the product.
Do NOT change business logic, analyzer logic, Stripe flow, pricing, routes (except where explicitly noted), or architecture.

PRIMARY GOAL
Address the remaining post-launch UX nitpicks:
1. mobile crowding of the example buttons
2. overly wide analysis text blocks on large desktop screens
3. missing breadcrumbs on article pages
4. article prose still reading too wide on desktop

NON-NEGOTIABLE CONSTRAINTS
- Do NOT change OpenAI prompts, rule engine, scoring, or analyzer behavior
- Do NOT change Stripe flow or payment logic
- Do NOT add features, accounts, dashboards, or persistence changes
- Do NOT migrate frameworks or add dependencies
- Do NOT materially rewrite existing page copy
- Do NOT alter SEO metadata except if strictly required for breadcrumbs markup, and even then keep existing metadata intact
- Preserve the current calm, document-centric visual direction already achieved
- Keep all changes minimal, cohesive, and low-risk

SUCCESS CRITERIA
- On narrow mobile screens, the “Try a good example” and “Try a bad deal example” controls no longer feel cramped
- On large desktop monitors, long explanatory text inside analyzer result cards is constrained to a comfortable reading width (~700–800px)
- Article pages include a simple breadcrumb trail near the top
- Article prose text is constrained to a readable width (~65ch / ~700px) even on large monitors
- No product behavior changes
- No visual regressions to the current polish pass

TASKS

1) Analyzer page — fix crowded mobile example buttons
File: client/src/pages/home.tsx

- Adjust the layout of the sample/example buttons so they do not feel cramped on narrow mobile screens
- Preferred approach: stack vertically on mobile and return to horizontal layout on larger screens
- Keep both buttons clearly secondary to the main input action
- Preserve current emphasis hierarchy:
  - “Try a good deal example” = lowest emphasis
  - “Try a bad deal example” = slightly more prominent secondary action
- Keep spacing calm and intentional

Implementation target:
- Use a responsive flex layout such as flex-col on mobile / flex-row on larger screens
- Add modest gap spacing
- Do not enlarge these buttons or make them compete with the primary CTA

2) Analyzer page — constrain long summary text width on desktop
File: client/src/pages/home.tsx
Also inspect any related analyzer result components if the explanatory text is rendered there.

- Constrain large text blocks inside the analyzer result cards so they do not span the full card width on large monitors
- Target reading width: approximately 700–800px max
- Apply this specifically to long explanatory text blocks such as:
  - “What This Deal Likely Means”
  - summary/explanation paragraphs
  - similar narrative result text
- Do NOT shrink tables, cards, or structured data layouts unnecessarily
- Only constrain the readable paragraph/text regions

Implementation target:
- Wrap long-form explanatory text in a max-w-[750px] or similar container
- Keep alignment visually consistent inside the card
- Preserve the current professional/objective card layout

3) Shared article layout — add breadcrumbs
File: client/src/components/ArticleLayout.tsx
Update article pages as needed if the layout needs breadcrumb props.

- Add a simple breadcrumb trail near the top of article pages
- Preferred structure:
  Home > Dealer Pricing Problems > Article Name
- “Home” should link to /
- “Dealer Pricing Problems” should link to /dealer-pricing-problems
- Current article title should appear as the final, non-linked breadcrumb item
- Style breadcrumbs subtly:
  - small text
  - muted foreground
  - clear spacing below before article content begins
- Breadcrumbs should improve orientation and internal navigation without becoming visually dominant

Implementation guidance:
- Keep this simple and reusable
- If needed, update ArticleLayout to accept a title prop for the current page/article title
- Do not alter the article heading/body copy itself
- Do not add complex schema or structured data unless already supported and trivial

4) Shared article layout / article prose — further constrain desktop reading width
Files:
- client/src/components/ArticleLayout.tsx
- inspect any shared article prose/container classes that affect all scenario/article pages

- Ensure article body text itself is constrained to a readable width on desktop
- Target width: around 65ch or ~700px
- This should apply specifically to prose/body reading content, not necessarily every full-width utility box or CTA block
- Preserve comfortable spacing and current typography improvements
- Keep utility boxes, CTA blocks, and structured sections visually balanced if they need a slightly different width
- Do not make the article layout feel cramped; just avoid long, exhausting line lengths

Implementation target:
- Apply a max-w-[700px], max-w-prose, or equivalent readable measure to the actual article text/prose container
- Center it appropriately
- Verify it still looks correct across all article/scenario pages using ArticleLayout

FILES TO TOUCH
At minimum, expect changes in:
- client/src/pages/home.tsx
- client/src/components/ArticleLayout.tsx

Potential additional touches:
- article/scenario pages only if needed to pass breadcrumb title props or adopt the improved prose container cleanly

DO NOT CHANGE
- analyzer logic
- scoring logic
- Stripe logic
- CTA copy unless needed for layout only
- SEO metadata
- route structure
- article body copy
- current calm visual system

OUTPUT WHEN COMPLETE
1. List every file changed
2. Summarize each fix implemented
3. Confirm how mobile example button crowding was solved
4. Confirm how analyzer text width was constrained
5. Confirm how breadcrumbs were added
6. Confirm how article prose width was constrained
7. Confirm that no product logic, scoring, payment flow, or pricing was changed