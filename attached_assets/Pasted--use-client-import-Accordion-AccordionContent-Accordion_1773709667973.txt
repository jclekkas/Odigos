"use client"

import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion"

const faqs = [
  {
    question: "What is an out-the-door price?",
    answer: "An out-the-door price is the total amount you will pay to drive the vehicle home. It includes the sale price, taxes, registration fees, documentation fees, and any dealer-added products. Many quotes show only a monthly payment or base price, which can obscure the true cost of the transaction.",
  },
  {
    question: "Why do some dealers avoid providing an out-the-door price?",
    answer: "Negotiating on monthly payments rather than total price allows more flexibility in how the deal is structured. A focus on monthly payments can make it easier to include add-ons or adjust financing terms in ways that may not be immediately apparent to the buyer.",
  },
  {
    question: "What does the review include?",
    answer: "The initial review provides a verdict, confidence level, and key concerns identified in the quote. The full analysis includes a complete list of issues, a checklist of missing information, a written response you can send to the dealer, and detailed reasoning for each concern.",
  },
  {
    question: "What happens to my quote after I submit it?",
    answer: "Your quote is reviewed and then deleted. Any personally identifiable information is redacted before processing. Anonymized pricing patterns may be retained to improve our analysis, but we do not store the original text of your submission.",
  },
  {
    question: "Is Odigos affiliated with any dealerships?",
    answer: "No. Odigos operates independently and has no business relationships with dealerships, lenders, or automotive manufacturers. The review is based solely on the information you provide.",
  },
  {
    question: "What if no issues are found?",
    answer: "If the review identifies no concerns, it means the quote appears to include standard pricing disclosures. This does not guarantee that the price is fair or that the vehicle is in good condition—only that the quote itself does not contain obvious disclosure issues.",
  },
]

export function FAQ() {
  return (
    <section id="faq" className="border-t border-border py-24 sm:py-32">
      <div className="mx-auto max-w-3xl px-4 sm:px-6">
        <h2 className="font-serif text-2xl font-semibold tracking-tight sm:text-3xl mb-12">
          Common Questions
        </h2>

        <Accordion type="single" collapsible className="w-full">
          {faqs.map((faq, index) => (
            <AccordionItem key={index} value={`item-${index}`}>
              <AccordionTrigger className="text-left text-sm font-medium">
                {faq.question}
              </AccordionTrigger>
              <AccordionContent className="text-sm text-muted-foreground leading-relaxed">
                {faq.answer}
              </AccordionContent>
            </AccordionItem>
          ))}
        </Accordion>
      </div>
    </section>
  )
}