We are making 3 surgical content-only improvements to the homepage to reduce confusion and increase trust. NO redesign. NO Stripe/pricing changes. NO routing. NO backend. Keep existing styling and layout structure as-is.

EDIT ONLY THIS FILE:
- client/src/pages/landing.tsx

GOALS
1) Add a simple “How it works” 3-step block (Paste → Analyze → Reply) that is scannable and minimal.
2) Add a privacy reassurance line near the dealer text input (“We don’t store or share your messages.”).
3) Add a tiny FAQ-style scope clarifier near the bottom (“We analyze pricing/terms, not vehicle condition.”).

HARD RULES
- Do not change the headline, subheadline, or pricing section copy.
- Do not change button destinations or Stripe logic.
- Do not add testimonials.
- Do not add new pages or routes.
- Do not introduce new design system components or animations.
- Use existing typography/color tokens (text-foreground, text-muted-foreground, border-border, bg-muted/50, etc.).

IMPLEMENTATION DETAILS

A) “How it works” block
- Place it ABOVE the existing “In minutes, you’ll know” section (or immediately after the hero/trust strip), whichever is currently first after the hero.
- Keep it lightweight: a short heading + 3 numbered lines.
- Example content (use exactly this wording):
  Heading: "How it works"
  Steps:
   1) "Paste the dealer text or quote"
   2) "Odigos flags what’s missing or risky"
   3) "Send the copy-paste reply before you go in"
- Styling guidance: same max width as surrounding sections; no icons required; use a simple ordered list or 3 rows with small numbered circles using existing muted background.

B) Privacy reassurance line near the dealer text box
- Place a single muted line directly under the dealer text input (or directly under the “Paste Dealer Communication” block / textarea).
- Exact text:
  "Privacy: Your messages are analyzed in real time. We don’t store or share them."
- Keep it small: text-xs or text-sm, text-muted-foreground.

C) Scope clarifier (mini FAQ)
- Place it near the bottom of the landing page just ABOVE the footer disclaimer.
- Add a small heading and one paragraph.
- Exact content:
  Heading: "What Odigos does NOT do"
  Body: "Odigos analyzes pricing transparency and financing terms. It does not evaluate vehicle condition, mechanical reliability, or accident history."

AFTER YOU IMPLEMENT
- Paste the updated landing.tsx (or at least the exact new sections + where they were inserted).
- Confirm only landing.tsx was modified.