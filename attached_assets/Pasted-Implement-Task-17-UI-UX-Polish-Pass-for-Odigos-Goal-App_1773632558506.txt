Implement Task #17 — UI/UX Polish Pass for Odigos.

Goal
Apply a tightly scoped visual, typography, and layout polish so Odigos feels calm, credible, and document-centric rather than like an “AI playground.”

This is not a redesign and not a feature project.

Non-negotiable constraints
- Do NOT change OpenAI prompts, analysis logic, scoring, or rule engine
- Do NOT change Stripe flow or payment behavior
- Do NOT add new features, accounts, dashboards, persistence, or workflows
- Do NOT change frameworks, routing architecture, or dependencies
- Do NOT materially rewrite page copy
- Do NOT change server routes except for fixing the /dealer-pricing-problems 404 by adding the intended page/route
- Preserve all current SEO metadata on existing article pages
- Preserve all current business logic and component behavior unless explicitly listed below

Definition of done
- Dark mode no longer shows aggressive bright-blue focus/ring glow on inputs and interactive elements
- Landing page hero heading has looser line-height and slightly looser tracking
- Landing page trust signals are more visually prominent near the top
- Landing page FAQ accurately reflects actual storage practices: PII-redacted text stored for 90 days for quality purposes
- Analyzer page header/logo is compact and polished, especially on mobile
- Analyzer example buttons have distinct secondary hierarchy:
  - “good deal” = lowest emphasis
  - “bad deal” = slightly more prominent secondary action
- DealScoreBadge is redesigned into a compact auditor-style verdict chip/header, not giant alarm-scale text
- LockedTier2Section appears inline in the result flow immediately after DetectedFieldsCard, not as a disconnected floating card
- All article/scenario pages use a shared layout component and shared bottom CTA block
- Article pages have a narrower reading column and better long-form readability
- /dealer-pricing-problems returns a real hub page instead of 404
- Mobile header footprint is reduced so the analyzer textarea sits higher above the fold

Tasks

1) CSS — reduce dark mode ring glow
File: client/src/index.css

In the .dark block:
- change --ring from 217 91% 50% to 217 45% 50%
- change --sidebar-ring from 217 91% 45% to 217 40% 45%

Do not change primary brand color usage elsewhere unless required by these variables.

2) Landing page — hero typography, trust signal lift, FAQ accuracy
File: client/src/pages/landing.tsx

- On the hero h1, replace leading-tight with leading-[1.25]
- Change tracking-tight to tracking-[-0.015em]
- Make the trust strip (“Independent · No account required …”) modestly more prominent via text weight and/or contrast
- Keep this tasteful; do not turn it into a loud banner
- Update the FAQ answer that currently says:
  “No. Messages are analyzed in real time and are not stored.”
  Replace it with a brief, accurate statement reflecting:
  PII-redacted text is stored for 90 days for quality purposes
- Keep the FAQ answer factual, calm, and brief
- Do not rewrite unrelated copy

3) Shared article layout component
Create: client/src/components/ArticleLayout.tsx

Requirements:
- accept children prop
- provide a sticky header
- use the Odigos logo at h-8 md:h-9
- use a compact, appropriately padded header container
- provide a main content area centered with max-w-2xl mx-auto
- allow article content to render inside this layout
- optionally include a subtle footer/privacy note only if cleanly supported
- replace duplicated article header/layout structure across scenario/article pages

Keep the component simple and reusable. Do not alter article copy.

4) Shared article CTA block
Create: client/src/components/ArticleCta.tsx

Requirements:
- styled CTA block with rounded-xl border bg-muted/30 p-6 mt-12
- headline: “Not sure if your dealer quote is complete?”
- short supporting line
- prominent filled button: “Check My Deal with Odigos”
- button links to /analyze

This should replace the current low-visibility end-of-article text CTA pattern.

5) Update all article/scenario pages to use shared components
Update these files to use ArticleLayout and ArticleCta:
- out-the-door-price.tsx
- monthly-payment-trap.tsx
- is-this-a-good-car-deal.tsx
- car-dealer-fees-explained.tsx
- dealer-doc-fee.tsx
- mandatory-dealer-add-ons.tsx
- dealer-pricing-tactics.tsx
- are-dealer-add-ons-mandatory.tsx
- dealer-added-fees-after-agreement.tsx
- market-adjustment-fee.tsx
- doc-fee-too-high.tsx
- dealer-changed-price-after-deposit.tsx
- finance-office-changed-the-numbers.tsx
- dealer-add-ons-list.tsx
- dealer-doc-fee-by-state.tsx
- car-dealer-fees-list.tsx
- calculate-out-the-door-price.tsx
- out-the-door-price-calculator.tsx
- car-dealer-fees-by-state.tsx

Rules:
- replace duplicated wrapper/header/main structure with ArticleLayout
- replace bottom CTA link with ArticleCta
- do NOT change article body copy
- do NOT change titles, headings, or SEO metadata
- do NOT introduce layout regressions or unrelated style changes

6) Analyzer page — compact header/logo
File: client/src/pages/home.tsx

- reduce header logo from h-28 to h-8 md:h-9
- reduce header container vertical padding from py-6 to py-3
- ensure mobile above-the-fold space improves, pushing the textarea higher
- keep the page looking intentional and not cramped

7) Analyzer page — sample button hierarchy
File: client/src/pages/home.tsx

Restyle:
- “Try a good deal example” => variant="ghost" size="sm"
- “Try a bad deal example” => variant="outline" size="sm"

Both should remain clearly secondary to the main paste/input action.

8) Analyzer page — DealScoreBadge redesign
Relevant analyzer/result component(s), likely including home.tsx and/or the badge component

Redesign the current score/verdict UI:
- remove the giant text-5xl / oversized alarm-style presentation
- replace it with a compact auditor-style verdict block
- include:
  - a colored GO / NO-GO / REVIEW-style chip
  - a clear verdict label in text-base font-semibold
  - a small muted confidence badge
- keep existing color semantics (green / yellow / red)
- keep logic unchanged
- this is visual restructuring only

9) Analyzer page — inline Unlock Analysis CTA
Relevant analyzer/result component(s)

- move LockedTier2Section so it renders inline in the result flow
- place it immediately after DetectedFieldsCard
- keep content unchanged
- keep Stripe/payment logic unchanged
- visually frame it as the natural next step in the analysis flow, not as a disconnected floating card

10) /dealer-pricing-problems hub page
Create: client/src/pages/dealer-pricing-problems.tsx
Update: client/src/App.tsx

Requirements:
- create a clean hub page for scenario/article navigation
- use ArticleLayout as the wrapper
- include proper setSeoMeta usage
- include data-testid attributes
- list all scenario/article pages with title + one-line description
- group them logically, such as:
  - fees
  - tactics
  - negotiation
- keep the page clean, useful, and consistent with the current site style
- register the route in App.tsx so /dealer-pricing-problems no longer 404s

Relevant files
- client/src/index.css
- client/src/pages/landing.tsx
- client/src/pages/home.tsx
- client/src/pages/market-adjustment-fee.tsx
- client/src/pages/dealer-doc-fee.tsx
- client/src/App.tsx

Plus all additional article files required by the shared layout/CTA migration.

Output format when complete
1. List every file changed
2. Summarize each implemented UI/UX improvement
3. Note exactly how /dealer-pricing-problems was fixed
4. Note anything intentionally left unchanged due to scope constraints
5. Confirm that no product logic, pricing flow, analysis logic, or Stripe behavior was changed