You are editing an existing repository. Be precise. Do not improvise architecture.

Task: wire the existing Playwright E2E tests into GitHub Actions CI.

The repository already has:
- a Playwright test suite in `tests/e2e/`
- an existing CI workflow in `.github/workflows/ci.yml`
- an app health endpoint at `/api/health`
- an existing database/migration setup already used by the app
- existing unit/integration tests that must remain untouched

Your job is to make the E2E suite run in CI against:
- a real PostgreSQL service container
- the real app server
- the real migrated schema

Non-goals:
- do not redesign the workflow
- do not rewrite tests unless absolutely required for CI compatibility
- do not refactor app code
- do not add Docker
- do not replace existing scripts with new systems
- do not remove or alter current CI jobs except where strictly necessary to add the new E2E job
- do not expand test coverage
- do not add narrative commentary

Follow these rules strictly:

1. Inspect the repository first and infer the actual existing commands.
   You must discover and use:
   - the package manager already in use
   - the real build command
   - the real start command used for production/server startup
   - the real migration command
   - whether Playwright config already exists and what port/baseURL it expects

2. Reuse existing commands.
   Examples:
   - if the repo uses `npm ci`, use that
   - if the repo uses `npm run build`, use that
   - if the repo uses `npm start`, use that
   - if the repo uses `npm run db:migrate`, use that
   These are only examples. Do not assume them if the repo says otherwise.

3. Add a new GitHub Actions job named `e2e` in `.github/workflows/ci.yml`.
   Requirements:
   - runs on `ubuntu-latest`
   - depends on the existing build job completing successfully
   - provisions PostgreSQL 16 as a service container
   - sets `DATABASE_URL` to the postgres service
   - sets `NODE_ENV=test`
   - installs dependencies
   - installs Playwright browsers with system deps
   - runs the real migration command against the test database
   - starts the real app server in the background
   - waits until `http://localhost:5000/api/health` is reachable
   - runs the existing Playwright suite
   - fails if Playwright fails

4. Environment handling:
   - include any env vars required for the server to boot in CI
   - prefer safe dummy/test values
   - do not require real production secrets unless the code absolutely cannot boot without them
   - if some app integrations must be disabled or stubbed via env vars already supported by the repo, use those existing env flags rather than code changes

5. Only make minimal supporting edits if required:
   - Playwright config: only touch it if CI would otherwise fail because baseURL/webServer/timeout is missing or wrong
   - package.json: only add a dependency/script if CI truly needs it and no equivalent already exists
   - workflow only otherwise

6. The implementation must be robust:
   - postgres service should have a health check
   - server startup should not race the test run
   - use a readiness wait step for `/api/health`
   - do not rely on sleep alone

7. Preserve local development behavior.
   - no breaking changes for local dev
   - no forced dependency on CI-only infra outside the workflow

Output requirements:
- First, show the full final contents of `.github/workflows/ci.yml`
- Then show any other files changed, in full if small, otherwise as exact unified diffs
- Do not explain the changes unless absolutely necessary for a non-obvious line

Success criteria:
- existing CI jobs still work
- new `e2e` job runs Playwright in CI
- app boots against real Postgres in CI
- schema is migrated before tests
- no unnecessary refactors