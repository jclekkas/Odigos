#32 - Phase 4: Light Theme + Header Wordmark
What & Why
Two issues prevent the app from matching the v0 target screenshot:

The app renders in dark/navy because class="dark" is hardcoded on the root <html> element in client/index.html. Since Tailwind dark mode is ["class"]-based, this forces every dark:* variant active across the entire app regardless of user preference. Removing this one attribute is the complete fix.

The header shows a PNG image logo (odigos_logo.png). The target shows a clean serif text wordmark "Odigos" instead.

Both are presentation-only corrections. No logic, routing, copy meaning, or business behavior changes.

Done looks like
The app presents in the warm cream light theme (matching the Phase 3 design system tokens) instead of the current dark/navy presentation
The header shows the serif text wordmark "Odigos" linked to home, with nav and CTA behavior preserved exactly
The old image logo import is removed cleanly from SiteHeader.tsx
No commit has been made — full diff is returned for review
Out of scope
Analyzer logic, upload, OCR, GPT, Stripe flows
Routes, API contracts, backend files
Any copy rewrites (section text, pricing, FAQ, CTA destinations)
Page structure redesigns
Dark mode behavior (the .dark CSS block in index.css stays intact; only the forced class on is removed)
Any file other than client/index.html and client/src/components/SiteHeader.tsx
Tasks
Remove forced dark class — In client/index.html, change <html lang="en" class="dark"> to <html lang="en">. This is the sole cause of the dark/navy rendering.

Replace image logo with serif wordmark — In client/src/components/SiteHeader.tsx, remove the odigos_logo.png import and the <img> element. Replace with a serif text wordmark styled to match the target: "Odigos" in font-serif, appropriate weight, linked to home. Preserve all other header behavior (nav links, CTA button, layout classes).

Return deliverables without committing — Produce the full unified diff, explain the two root causes and fixes, confirm no backend/logic/route files were changed, and confirm no commit was made.

Relevant files
client/index.html:2
client/src/components/SiteHeader.tsx
Dependencies
Phase 3: Design System Warm Cream Alignment