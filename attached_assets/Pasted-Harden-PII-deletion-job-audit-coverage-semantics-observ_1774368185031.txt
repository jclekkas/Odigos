Harden PII deletion job (audit coverage, semantics, observability)
What & Why
Task #91 successfully implemented a working 90-day PII cleanup job. However, several gaps remain that create compliance and operational risk:
The audit surface is incomplete (“all other tables not touched” is not sufficient)
retentionExpiresAt is being reused with ambiguous meaning
Logs do not expose eligible vs skipped rows (silent failure risk)
The exact 90-day cutoff operator is not explicitly defined
Downstream warehouse safety after redaction is not verified
Non-table PII surfaces (logs, upload flow, etc.) may not be accounted for
This task is a hardening pass, not a redesign.
Goal
Make the PII cleanup system fully explicit, auditable, and defensible with zero ambiguity and no hidden PII retention paths.
Done looks like
piiCleanup.ts includes a complete audited inventory of all PII surfaces checked, not just tables touched
Every surface has an explicit decision: delete, redact, or exclude (with reason)
retentionExpiresAt is no longer ambiguous OR is explicitly safeguarded in code
Structured logs include:
scannedCount
eligibleCount
clearedCount
skippedCount
dryRun
cutoff
Exact cutoff rule is explicitly defined and consistent across code + tests
Warehouse/materialized views continue working after redaction (verified)
Non-table PII surfaces are explicitly audited and dispositioned
Acceptance report is complete and explicit (no assumptions)
Out of scope
Schema redesign (unless absolutely unavoidable)
Privacy portal / user-facing features
New retention policies
Broad logging refactor
Product/UX changes
Tasks
1. Replace “NOT TOUCHED” with a real audit
Update the comment block in:
server/jobs/piiCleanup.ts
You must explicitly list ALL surfaces reviewed, including:
public.dealer_submissions
raw.user_analyses
every other application table reviewed
warehouse tables (raw/core)
logs (request + error)
upload / file extraction flow
analysis output persistence
any external metadata (e.g., Stripe if applicable)
For EACH:
can it contain user-submitted or identifying data?
is it touched by the cleanup job?
decision: delete / redact / exclude
reason
Do NOT leave generic statements like “all other tables not touched.”
2. Fix or explicitly guard retentionExpiresAt
Current state:
field is reused as a “cleared-at” marker
This is semantically unsafe.
Do ONE:
Option A (preferred)
If possible within constraints, avoid overloading the field.
Option B (if reuse must remain)
Add explicit comment in:
job header
write logic
State clearly:
this field no longer represents expiry after cleanup
Add a guard so future logic cannot treat it as an expiry field
Then:
search the codebase for all reads of retentionExpiresAt
confirm none rely on it as an active expiry after cleanup
document findings in acceptance report
3. Add full observability (required)
Update logs so each table emits:
scannedCount
eligibleCount
clearedCount
skippedCount
dryRun
cutoff
Keep existing log event names.
Goal: prevent silent WHERE-clause bugs.
4. Define cutoff operator explicitly
Pick ONE and standardize everywhere:
submittedAt < cutoff
OR
submittedAt <= cutoff
Then:
implement consistently
update tests
document in code
include in acceptance report
No ambiguity.
5. Verify warehouse safety
Check:
server/warehouse/scheduler.ts
server/warehouse/warehouseWriter.ts
shared/warehouse.ts
Ensure:
redacted rows do NOT break refresh jobs
materialized views do NOT fail
null text fields are handled safely
analytics queries still function
If any query assumes non-null text → fix it.
Do NOT redesign warehouse logic.
6. Audit non-table PII surfaces
Explicitly review:
request logs
error logs
upload parsing pipeline
temp files
analysis outputs
third-party metadata fields (if used)
For each:
confirm no persistence OR
document exclusion OR
fix a clear retention issue if tightly scoped
Do NOT expand into a large refactor.
7. Extend tests
Add coverage for:
exact cutoff operator behavior
eligible vs skipped counts
logging fields presence
idempotency with new counters
retentionExpiresAt guard behavior
null-safe downstream handling (if applicable)
Relevant files
server/jobs/piiCleanup.ts
server/scripts/runPiiCleanup.ts
tests/unit/piiCleanup.test.ts
server/warehouse/scheduler.ts
server/warehouse/warehouseWriter.ts
shared/warehouse.ts
shared/schema.ts
server/piiRedact.ts
server/index.ts
Constraints
Do NOT redesign schema unless unavoidable
Do NOT change product behavior
Do NOT introduce new systems
Keep changes tightly scoped
Be explicit — no assumptions
Acceptance report (required)
Output:
every PII surface reviewed
every surface touched
delete vs redact vs exclude decision per surface
exact cutoff rule + operator
status of retentionExpiresAt (overloaded or fixed)
full logging fields now emitted
warehouse/materialized view verification results
any remaining risks or technical debt