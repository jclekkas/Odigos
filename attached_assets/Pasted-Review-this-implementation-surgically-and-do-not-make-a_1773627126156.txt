Review this implementation surgically and do not make any changes yet.

I want a careful code review of Upload v1 only.

Scope of the implemented feature:
- server/extractText.ts — new extraction helper
- server/routes.ts — multer setup + POST /api/extract-text
- client/src/pages/home.tsx — upload affordance below the existing textarea
- Added dependencies: multer, @types/multer, pdf-parse, @types/pdf-parse

Expected behavior:
- User clicks “or upload a screenshot or PDF”
- Client validates file type and 10 MB max size before any network call
- Accepted types: PNG, JPG, JPEG, WEBP, PDF
- File is sent as multipart to POST /api/extract-text
- Server extracts text:
  - images → existing GPT-4o vision path
  - PDFs → pdf-parse first, with GPT-4o fallback if embedded text is sparse
- Returned text populates the existing dealerText textarea
- User edits if needed and submits normally
- Existing analysis pipeline must remain completely unchanged

Your task right now:
1. Inspect the actual code that was changed
2. Identify any hidden risk, bug, edge case, or architectural sloppiness
3. Focus especially on:
   - whether the existing paste-text flow is truly untouched
   - whether the PDF fallback path is actually robust and supported
   - whether multipart handling is correct and safe
   - whether uploaded files are ever written to disk
   - whether the textarea remains editable after upload
   - whether error handling is calm and non-crashing
   - whether there are any mobile/layout regressions from the upload affordance
   - whether there are any TypeScript issues, nullability issues, or unsafe assumptions
   - whether the client properly clears loading/error state across success and failure paths
   - whether large or malformed files could break the endpoint
   - whether server-side MIME validation is strict enough
   - whether the route could interfere with any existing middleware or body parsing

Output format:
- “Looks safe” or “Not safe yet”
- Then list:
  1. Critical issues
  2. Medium-risk issues
  3. Minor cleanups
  4. Exact fixes recommended

Important constraints:
- Do NOT broaden scope
- Do NOT suggest redesigns
- Do NOT suggest new features
- Do NOT touch pricing, analysis prompt, checkout, results, routes, or article pages
- Stay tightly focused on Upload v1 only

If the implementation is safe, say so clearly.
If it is not safe, be specific and concrete.