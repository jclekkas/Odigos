Create 5 new SEO article pages for the Odigos site covering car dealer fees by state, using a shared template component and a centralized data file.

CRITICAL CONTEXT:
Odigos is a consumer protection tool that analyzes dealer quotes and identifies hidden costs, inflated fees, and pricing manipulation.

These pages target high-intent geo queries like:
- "dealer doc fee California"
- "car dealer fees Florida"
- "doc fee Texas"

Each page must feel UNIQUE and tailored to the state — not like a template with swapped numbers.

---

STATES TO CREATE:

1. California
2. Texas
3. Florida
4. New York
5. Georgia

---

ARCHITECTURE REQUIREMENT (IMPORTANT):

- Create ONE shared page template component (e.g. StateFeesTemplate)
- Create ONE data file (e.g. stateFeesData.ts)
- Each state page imports the template and passes in its state-specific data

DO NOT create 5 completely separate page implementations.

---

CRITICAL DIFFERENTIATION RULE:

Each state must have a distinct narrative angle:

- California → strong consumer protection (doc fee cap ~ $85)
- Florida → high uncapped doc fees ($499–$999+), high risk of overpaying
- Texas → no cap, negotiation is the only leverage
- New York → additional local taxes/surcharges increase total cost unexpectedly
- Georgia → Ad Valorem Title Tax replaces sales tax and confuses buyers

If the pages feel interchangeable, rewrite until clearly distinct.

---

PAGE STRUCTURE (FOR ALL STATES):

- H1: "Car Dealer Fees in [State]"
- Short intro (state-specific, no generic phrasing)
- "Typical Fees in [State]" section
- "Doc Fee Rules in [State]" section
- "What to Watch Out For in [State]" (must be state-specific)
- "Example Breakdown" (state-specific numbers)
- "What to Say to the Dealer in [State]" (copyable message)
- Mid-article CTA
- Bottom ArticleCta

---

STATE-SPECIFIC CONTENT REQUIREMENTS:

Each page must include:

1. Realistic fee ranges:
- doc fee range
- tax structure (sales tax vs other)
- title/registration

2. Unique warnings:
- California: watch for add-ons replacing capped fees
- Florida: extremely high doc fees
- Texas: price negotiation matters more than fee caps
- New York: local taxes increase OTD significantly
- Georgia: misunderstanding of TAVT vs sales tax

3. Example:
- A realistic OTD breakdown with state-specific numbers

---

INTERNAL LINKING (MINIMUM 4 LINKS PER PAGE):

Each page must include:

OTD:
- /out-the-door-price
- /what-does-out-the-door-price-include

Fees:
- /what-is-a-dealer-doc-fee
- /are-dealer-fees-negotiable
- /hidden-dealer-fees

Add-ons:
- /are-dealer-add-ons-mandatory
- /dealer-add-ons-explained

Use natural anchor text.

---

HUB PAGE UPDATE (IMPORTANT):

Update:
/car-dealer-fees-by-state

Add links to all 5 new state pages with clear anchor text:
- "California dealer fees"
- "Florida dealer fees"
- etc.

---

CTA REQUIREMENTS:

Mid-article CTA:
- tailored to state context
- example:
  "If you're looking at a quote in [State], Odigos checks whether these fees are normal..."

Bottom CTA:
- standard ArticleCta
- emphasize:
  • fast analysis
  • no signup
  • avoid overpaying

---

SEO REQUIREMENTS:

Each page must:
- use setSeoMeta
- title ≤ 60 chars (e.g. "California Dealer Fees | Odigos")
- meta description 140–155 chars
- include state + keyword naturally

---

IMPLEMENTATION:

- use ArticleLayout
- use ArticleCta
- follow existing page patterns
- register routes in App.tsx
- add routes to scripts/prerender.mjs

---

SELF-CHECK BEFORE FINISHING:

For each page confirm:
- intro is state-specific (not generic)
- warnings are unique to that state
- example uses realistic state numbers
- internal links are valid
- content does not feel templated
- page is useful enough to stand alone

---

GOAL:

These pages should:
- rank for state-specific queries
- feel authoritative and localized
- reinforce the broader pricing system (OTD + fees + add-ons)
- drive users toward the Odigos analyzer

---

Generate all 5 state pages now using the shared template + data model.