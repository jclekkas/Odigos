#87 - Fix Warehouse Schema in Production + Restart Dev

What & Why
The production database is missing the entire warehouse schema: both raw and core schemas, all warehouse tables, and all materialized views. Every user analysis since launch has silently failed to write to the warehouse. Root cause: scripts/post-merge.sh runs db:push interactively and aborts when it detects the materialized views would be dropped — so the push never completes in production. The dev server also needs a restart after a port conflict from the last merge.

Done looks like
- Dev preview is running again with no port conflict error
- Production no longer logs raw.user_analyses does not exist
- All warehouse tables exist in production:
  - raw.user_analyses
  - raw.enforcement_records
  - core.dealers
  - core.listings
  - core.consumer_complaints
  - core.states
  - core.metro_areas
- Materialized views exist in production:
  - core.platform_metrics
  - core.national_stats
  - core.state_stats
- Reference data seeded:
  - 51 states
  - 25 metro areas
- Future deploys no longer abort on the schema push prompt

Out of scope
- Re-running historical backfills in production
- Changing warehouse table definitions
- Redesigning the warehouse model

Tasks

1) Fix post-merge.sh for non-interactive schema push
Before running db:push, explicitly drop:
- core.platform_metrics
- core.national_stats
- core.state_stats

Use schema-qualified names and IF EXISTS.

Then run db:push non-interactively using --accept-data-loss or equivalent.

After a successful push, immediately re-run the warehouse view setup so the materialized views are recreated.

Requirements:
- Script must be fully non-interactive
- Script must fail clearly on any real error
- Script must be fully repeatable
- View recreation must happen only after a successful db:push

2) Add ensureWarehouseSchema() to server startup
In server/index.ts, before the server begins listening, call a new ensureWarehouseSchema() function.

Behavior:
- Check whether raw.user_analyses exists
- If it exists:
  - skip bootstrap
  - log clearly that warehouse schema already exists
- If it does not exist:
  - create schemas raw and core if missing
  - create all warehouse tables
  - recreate materialized views
  - seed core.states
  - seed core.metro_areas

Requirements:
- Reuse definitions from shared/warehouse.ts and existing setup utilities where possible
- Must be idempotent
- Must be safe to run on every startup
- Must be fully wrapped in try/catch
- Must log clearly
- Must never crash the server on failure
- Server startup must continue even if warehouse bootstrap fails

3) Restart dev workflow
After the above changes, restart the Start application workflow to clear the port conflict and confirm the dev server starts cleanly.

Relevant files
- scripts/post-merge.sh
- server/index.ts
- server/warehouse/setupViews.ts
- server/warehouse/seedReference.ts
- shared/warehouse.ts

CRITICAL IMPLEMENTATION RULES
- Do NOT only describe the solution — actually implement it
- Do NOT skip steps as “already handled”
- Do NOT return a completion summary without code changes
- Show the exact code changes for:
  - scripts/post-merge.sh
  - server/index.ts

VERIFICATION REQUIREMENTS
Before returning, confirm:

1. scripts/post-merge.sh
- Drops the 3 materialized views using schema-qualified names
- Runs db:push non-interactively
- Recreates the views after a successful push

2. ensureWarehouseSchema()
- Actually checks for raw.user_analyses existence with a real query
- Creates schemas and warehouse tables if missing
- Reuses existing warehouse setup logic where possible
- Recreates materialized views
- Seeds core.states and core.metro_areas
- Is wrapped in try/catch
- Does not crash the server on failure

3. Startup flow
- ensureWarehouseSchema() runs before app.listen/server.listen

4. Dev server
- Start application workflow runs successfully
- No port conflict remains

Return:
- Exact files changed
- Exact code changes
- The warehouse existence-check query
- Confirmation that the dev preview is running again