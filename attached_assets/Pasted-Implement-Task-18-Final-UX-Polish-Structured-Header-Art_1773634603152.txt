Implement Task #18 — Final UX Polish: Structured Header + Article Improvements

Goal
Resolve the remaining usability gaps identified after Task #17 by adding a consistent structured header, breadcrumb navigation on article pages, tighter prose width, less cramped mobile example buttons, a calm explainer above the analyzer textarea, and a visible methodology/standards link that explains how Odigos evaluates dealer quotes.

This is a focused layout and typography pass only.
Do NOT redesign the product.

Non-negotiable constraints
- Do NOT change analyzer logic, scoring, prompts, or rule engine
- Do NOT change Stripe flow, pricing, or payment behavior
- Do NOT change backend routes or server architecture
- Do NOT add dependencies
- Do NOT change frameworks
- Do NOT redesign the logo asset itself
- Do NOT add navigation beyond the two specified links
- Do NOT materially rewrite copy except for the exact analyzer context section specified below
- Do NOT alter article body copy, headings, or SEO metadata
- Keep all changes strictly to layout, component structure, spacing, and typography

Definition of done
- Every relevant page uses a consistent full-width SiteHeader
- The Odigos logo sits inside a visually grounded header container instead of floating on the page
- The logo links to /
- Header nav contains exactly two links:
  - Scenario Hub → /dealer-pricing-problems
  - How It Works → /#how-it-works
- Article pages show breadcrumbs:
  Home / Dealer Pricing Problems / [Article title]
- Article prose is constrained to ~700px max width
- Analyzer example buttons stack vertically on mobile and sit in a row on desktop
- “What This Deal Likely Means” narrative text is constrained to max ~750px on large monitors
- Above the analyzer textarea there is a calm heading and one-line description
- A small documentation-style link appears near the analyzer explaining how the analysis works
- No analyzer logic, scoring, prompts, Stripe flow, pricing, or routing behavior changes

Tasks

1) Create SiteHeader component
Create:
client/src/components/SiteHeader.tsx

Requirements:
- full-width header
- sticky positioning
- bottom border
- logo aligned left
- two nav links aligned right
- logo uses existing transparent asset:
  attached_assets/odigos_logo.png
- logo size: h-8 md:h-9
- logo links to /

Nav links:
- Scenario Hub → /dealer-pricing-problems
- How It Works → /#how-it-works

Nav link styling:
- text-sm
- text-muted-foreground
- hover:text-foreground

Keep the header calm and lightweight.
Do NOT introduce a large marketing navbar.

2) Update ArticleLayout to use SiteHeader and breadcrumbs
File:
client/src/components/ArticleLayout.tsx

Requirements:
- replace current inline header with <SiteHeader />
- add breadcrumb trail above the article body
- ArticleLayout should accept a required title prop used only for breadcrumb display
- breadcrumb structure:
  Home / Dealer Pricing Problems / [title]
- “Home” links to /
- “Dealer Pricing Problems” links to /dealer-pricing-problems
- current article title is plain text, not clickable
- breadcrumb styling should be subtle and compact

Layout requirements:
- apply max-w-[700px] mx-auto to the main prose/content container
- keep CTA blocks and utility sections visually balanced
- preserve current article structure and content

3) Update all article pages to pass title prop
Update all article pages that use <ArticleLayout> so they pass:

title="..."

The title value must match that page’s existing h1 text exactly.
The title prop is for breadcrumb display only.

Use an automated/scripted update if helpful, but avoid content drift.

4) Update analyzer page
File:
client/src/pages/home.tsx

Replace the current inline header with <SiteHeader />.

Add a context section between the header and the textarea card.

Use this exact content:

Heading:
Analyze a Dealer Quote

Description:
Paste a dealer quote, email, or text message. Odigos checks for missing out-the-door pricing, hidden fees, and common dealership tactics.

Style guidance:
- calm instructional tone
- max-w-2xl mx-auto
- spacing mb-6

5) Add methodology / standards link near analyzer
File:
client/src/pages/home.tsx

Add a small documentation-style link near the analyzer context section pointing to:

/how-odigos-works

Link text:
How this analysis works

Placement guidance:
- directly below the context description or near the textarea card
- visually subtle but discoverable

Styling guidance:
text-sm
text-muted-foreground
hover:text-foreground
underline-offset-4 hover:underline

This should feel like documentation for a serious product, not a CTA button.

Do NOT create the methodology page itself if it already exists. If it does not exist yet, leave the link pointing to the route for now.

6) Improve mobile layout of example buttons
File:
client/src/pages/home.tsx

Update example buttons layout:

mobile:
flex-col

desktop:
flex-row

Add modest spacing.

Button hierarchy must remain:
good example → variant="ghost"
bad example → variant="outline"

7) Constrain analyzer narrative text width
File:
client/src/pages/home.tsx

Locate narrative text blocks such as:

“What This Deal Likely Means”

Wrap these sections in a container:

max-w-[750px] mx-auto

Only constrain narrative paragraphs.
Do NOT shrink tables or structured analysis sections.

8) Verify hub page compatibility
File:
client/src/pages/dealer-pricing-problems.tsx

Ensure the page renders correctly with the shared SiteHeader.

Do not rewrite page content unnecessarily.

Relevant files
- client/src/components/ArticleLayout.tsx
- client/src/components/SiteHeader.tsx
- client/src/pages/home.tsx
- client/src/pages/dealer-pricing-problems.tsx
- attached_assets/odigos_logo.png

Plus all article pages using ArticleLayout.

Output when complete
1. List every file changed
2. Confirm SiteHeader implementation
3. Confirm breadcrumb behavior
4. Confirm article prose width constraint
5. Confirm analyzer context section placement
6. Confirm mobile example button layout fix
7. Confirm narrative text width constraint
8. Confirm methodology link placement
9. Confirm no analyzer logic, prompts, Stripe flow, pricing, or routing behavior changed